---
created: 2026-09-23T06:10:37.108Z
session: f14b6b2d-494d-46df-9e5f-c80b5daa2e12
working_directory: C:\Users\2000167629\Vibe-Analytics-Phase3
---


## Plan: Context Card + Portfolio Health + Anomaly Table Fixes

### 1. BrightwayIntro Redesign (`BrightwayIntro.tsx`)
- **Bold summary line**: Make "$2.2B revenue · 40 stores · 5 regions · 3 departments" visually prominent with larger font, bold, highlighted
- **Add Time Context**: New section with live dates from Snowflake. Will add a `timeContext` prop fetched from a new API endpoint
- **Remove "Three converging forces"**: Delete the entire FORCES section
- **Keep "7-14 days → one morning" value prop**

### 2. Time Context API (`route.ts`)
- Add new query to fetch date boundaries: `MAX(FISCAL_WEEK)` from DT_DEMAND_WEEKLY, `MIN/MAX` date ranges
- Return as part of AnalyticsData or as a new field

### 3. KPI Card Redesign (`page.tsx` — KPICard component)
- Premium dashboard styling: larger values, better typography, gradient accents, consistent card heights
- Move KPIs to occupy the space freed by removing "converging forces"
- Better color contrast and visual hierarchy

### 4. Portfolio Health Overview (`VarianceHistogram.tsx`)
- **Text fix**: "Anomaly ≥20%" → "Anomaly beyond ±20%"
- **Merge metrics into chart**: Remove the separate Portfolio Health panel; overlay/embed Total SKUs, Mean, Median, Std Dev, Skew as a summary strip within the chart container. Make the chart wider.

### 5. AnomalyTable Fixes (`AnomalyTable.tsx`)
- **Filter label**: "All Categories (L2)" → "All Categories & Regions (L2)"
- **Default top 10**: Limit L2 aggregated view to top 10 by value_at_risk
- **Region column fix**: L2 aggregated rows show `regionCount` (a number) instead of region names. Fix to show comma-separated region names or "7 regions" text.
- **Deduplicate L3 rows**: When drilled into L3, keep only the highest VALUE_AT_RISK_USD row per L3+Region combo
- **Top 10 per L3 view**: Limit to top 10 rows after dedup, sorted by deviation then impact

### Files Modified (5)
1. `BrightwayIntro.tsx` — Redesign with bold summary, time context, remove forces
2. `route.ts` — Add time context query
3. `page.tsx` — KPICard redesign, pass time context to BrightwayIntro
4. `VarianceHistogram.tsx` — Text fix, merge metrics into chart
5. `AnomalyTable.tsx` — Filter label, top 10 default, region display, dedup, ranking


---

## User Context

do it robustly without messing anything else