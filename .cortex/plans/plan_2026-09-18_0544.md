---
created: 2026-09-18T05:44:23.714Z
session: f14b6b2d-494d-46df-9e5f-c80b5daa2e12
working_directory: C:\Users\2000167629\Vibe-Analytics-Phase3
---


# Autonomous Module Report Redesign — Implementation Plan

## Context
- Part A (deploy batch) is blocked — files not on stage yet
- Current backend: 2 personas ("Supply Chain Director" works, "Director of Demand Planning" = DATA GAP)
- Target: 3-persona model (Demand Planner, Supply Planner, Director of Demand Planning) with per-persona tabs

## Approach
Build the full 3-persona frontend NOW using **SQL fact table queries for visualizations** + **pipeline for AI narratives**. Map personas temporarily to existing backend personas until Part A lands. When Part A is deployed, swap the backend mappings with minimal code change.

**Temporary mapping until Part A:**
- **Demand Planner** → runs pipeline as "Supply Chain Director" (works) + SQL filtered to all 3 depts at category level
- **Supply Planner** → runs pipeline as "Supply Chain Director" (works) + SQL filtered to all depts at supply/replenishment level  
- **Director of Demand Planning** → runs pipeline as "Supply Chain Director" (works) + SQL unfiltered enterprise roll-up

---

## Phase 1: Persona Model + Constants

1. **Update `constants.ts`**: Change PERSONAS to `["Demand Planner", "Supply Planner", "Director of Demand Planning"]` in this exact sequence
2. **Add persona metadata**: Define per-persona config (department scope, detail level, storyboard role, pipeline backend mapping)
3. **Update `AppContext.tsx`**: Align context types to new persona model
4. **Update `MODULE_BULLETS`** for 3 new personas

## Phase 2: Report Structure — Per-Persona Tabs

5. **Redesign `page.tsx` layout**: Replace single scrollable report with tabbed persona sections
   - Sticky persona tab bar at top of report area
   - Each tab renders independently with its own content
   - "Run Pipeline" button per-persona (runs orchestration for that persona)
6. **Brightway Retail intro section**: At report start (above tabs), add the "Setting the Stage" blurb — Brightway Retail context, what converging forces are at play, and the value proposition (7-14 days → one morning). No individual names, only persona roles.
7. **Per-persona intro card**: At the top of each tab, a card explaining what this persona does, their scope, and what the report will show them
8. **Persona handoff footer**: At the bottom of each tab, a "handoff" transition card pointing the user to the next persona's tab

## Phase 3: Demand Planner Tab (Steps 1-2: DETECT + EXPLAIN)

Per the storyboard flow (slides 5-9):

9. **Step 1 — DETECT section**:
   - **Heatmap**: Default shows department-level aggregation (3 departments × 5 regions). Add dropdown filter to drill into sub-categories within a department. Clean grid like slide 6.
   - **Ranked Anomaly Table**: Styled like slide 7 — rank column, sub-category, region, deviation with ↑/↓ arrows, impact $, days running, direction (Over/Under badges), confidence (High/Medium/Low). "HOW TO READ IT" sidebar.
   - **Variance Histogram**: Per slide 8 — colored zones (green normal, amber watch, red anomaly), portfolio health stats panel, "HOW TO READ IT" sidebar.
   - **Cross-Department Signal Strip**: Per slide 9 — one card per department showing top anomaly, portfolio size, anomaly counts. Common signal forces row at bottom (Weather, Promotion, Competitor, Digital).
   - Each chart has: static "HOW TO READ IT" sidebar + AI narrative paragraph below

10. **Step 2 — EXPLAIN section**:
    - **Driver Attribution chart**: Stacked horizontal bars showing signal decomposition (Weather, Promo, Competitor, Digital, Residual) per anomaly. Confidence per signal. Multicollinearity warnings.
    - **Sub-Category Breakdown**: Table showing which sub-categories contribute to the deviation
    - AI narrative explaining the root cause analysis

## Phase 4: Supply Planner Tab (Steps 3-4: PREDICT + ACT)

11. **Step 3 — PREDICT section**:
    - **Recovery Timeline / Demand Trajectory**: 14-day projection chart showing expected demand decay by driver
    - **Stockout Risk Table**: Store-SKU level availability predictions with probability scores
    - AI narrative on how long the spike lasts and what breaks if nothing is done

12. **Step 4 — ACT section**:
    - **Recommended Actions cards**: Each action as a card with: what to do, quantity, cost, impact protected, authority needed, approval status
    - **Cost-Benefit Summary**: Total protected vs total cost, per-action breakdown
    - AI narrative on sequenced recommendations within guardrails

## Phase 5: Director of Demand Planning Tab (Step 5: COMMUNICATE)

13. **Executive Briefing Pack**:
    - **Enterprise KPI banner**: High-impact anomalies count, net revenue at stake, protected/recovered amount, expedite cost, pending approvals
    - **Consolidated Enterprise View table**: Per-department row showing signal, at-stake, protected, status (with approval badges)
    - **Cross-Department Contentions**: Flagged conflicts needing director resolution
    - **Closing the Loop**: Fed-back-into-future-sensing items, S&OP headline
    - AI narrative summarizing the full week

## Phase 6: Visualization Enhancements (All Tabs)

14. **"HOW TO READ IT" sidebar component**: Reusable component that sits to the right of each chart with static bullet points explaining the visualization in business-friendly language
15. **Chart explanation text**: Below each chart, a plain-English paragraph explaining: "What is this showing?", "Deviation with respect to what?" (7-day actuals vs forecast baseline), "Why does it matter?"
16. **Filters**: Department dropdown on heatmap, category filter where applicable, all scoped to the persona's department scope
17. **Responsive layout**: Chart + sidebar on desktop, stacked on mobile

## Phase 7: API + Data Layer Updates

18. **New analytics API routes per persona**: Different SQL queries per persona type (Demand Planner gets category-level, Supply Planner gets supply metrics, Director gets enterprise roll-ups)
19. **Pipeline mapping layer**: Map new persona names to existing backend personas. When Part A lands, swap this one mapping.
20. **Cross-department signal strip API**: New endpoint aggregating top anomaly per department

## File Changes Summary
- `constants.ts` — new personas, metadata
- `AppContext.tsx` — aligned to 3-persona model
- `app/autonomous/page.tsx` — full redesign with tabs
- `components/autonomous/` — enhanced/new chart components with HOW TO READ IT sidebars
- `app/api/autonomous/analytics/route.ts` — persona-scoped queries
- `app/api/autonomous/submit/route.ts` — updated persona allowlist + backend mapping
- `lib/api.ts` — updated types
- `lib/orchestration-types.ts` — new types for cross-dept strip, executive briefing


---

## User Context

But I want you to Ask a clarifying question regarding the two latency wise .. I think it will add up to more latency if we run one persona per pipeline. Better to run all 3 together I think for our demo needs. In real world production instance, we wouldn't do it this way.first answer this and then Pipeline runs for all 3, so all data artifacts are created together. Only the ones specific to the selected persona in the drop down are consumed and made into the report.You're right, but we won't use the persona drop down really for the interactive part of the demo... whatever it is can stay, it's a different CoWork agent so there's no issues with data gathering based on the specific question & personaPersona selected becomes irrelevant for interactive module.Answer and generate a new plan accordingly