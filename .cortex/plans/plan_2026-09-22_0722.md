---
created: 2026-09-22T07:22:01.415Z
session: f14b6b2d-494d-46df-9e5f-c80b5daa2e12
working_directory: C:\Users\2000167629\Vibe-Analytics-Phase3
---


## Implementation Plan — Autonomous Report UX Overhaul

### 1. Remove Persona Tabs from Report (page.tsx)
- Delete the inline persona tab bar (lines 887-908)
- Remove `PersonaHandoff` components from all 3 persona sections
- Remove `PersonaBlurb` component from the report
- Keep header-level persona dropdown (AppContext) — it still controls which steps are visible
- The report flows as a unified experience; persona switching happens only via the global header

### 2. Reorder Detect Visualizations + Auto-Numbering (page.tsx + all viz components)
- Reorder Detect section to: **Heatmap → Variance → Anomaly → CrossDept**
- Create a **`VizNumbering`** system:
  - Each visualization component accepts a `vizNumber` prop (e.g., `"1.1"`, `"1.2"`)
  - Build a shared `SectionedTitle` component pattern OR pass the number into each viz's existing h3 title
  - Numbers are computed from step + index (e.g., Step 1 Detect: 1.1, 1.2, 1.3, 1.4; Step 2 Explain: 2.1, 2.2)
  - Future visualizations automatically get the next number in sequence

**Files**: page.tsx, DeviationHeatmap, VarianceHistogram, AnomalyTable, CrossDeptSignalStrip, DriverAttribution, RecoveryTimeline, StockoutRiskTable, ExecutiveBriefingPack

### 3. Convert Filters to Dropdowns (DeviationHeatmap, DriverAttribution)
- **DeviationHeatmap**: Replace toggle buttons with a single `<select>` dropdown — options: "All Departments", each L1 department
- **DriverAttribution**: Replace toggle buttons with `<select>` dropdown — options: "All Departments", each department
- Consistent dropdown styling: visible text, no truncation, matching border/font across all sections

### 4. Driver Attribution Overhaul (DriverAttribution.tsx + route.ts + types)
- **API**: Add `CATEGORY_L2` to `fetchDrivers()` query via JOIN DIM_PRODUCT
- **Types**: Add `category_l2: string` to `DriverRow` interface
- **Default L2 view**: Aggregate data at L2 level by default (sum/avg contributions by L2 category)
- **L3 drill-down**: Add dropdown filter to drill from L2 → L3 subcategories
- **Legend placement**: Move from below chart (`y: -0.15`) to above chart (`y: 1.05` or top orientation)
- **Axis improvements**: Increase left margin, add standoff between Y-axis title and labels, ensure no overlap
- **Confidence per category**: Aggregate confidence at L2 level (weighted average or min), display actual values — not all 100%. Data shows varied confidence: Weather 0-100%, Competitor 0-100%, Promotion 35.6-100%, Residual 0-96.8%

### 5. Recovery Timeline — Dedicated Sub-section + Filters (RecoveryTimeline.tsx + route.ts + types + page.tsx)
- **Position**: Move Recovery Timeline from Explain section to a visually separated sub-section between Explain and Act (still under Step 2, but with its own sub-header)
- **API**: Add `CATEGORY_L2` to `fetchRecovery()` via JOIN DIM_PRODUCT
- **Types**: Add `category_l2: string` to `RecoveryPoint` interface
- **Default L2 view**: Show L2 categories by default in the trajectory chart
- **Filters**: Add dropdown filter to drill into L3 subcategories
- **Layout**: Ensure title, legend, and axis labels are visible without scrolling (legend above chart)

### Files Modified (10 files)
1. `frontend/lib/orchestration-types.ts` — Add `category_l2` to DriverRow, RecoveryPoint
2. `frontend/app/api/autonomous/analytics/route.ts` — Add L2 joins for drivers + recovery
3. `frontend/app/autonomous/page.tsx` — Remove persona tabs/handoffs, reorder vizs, add numbering, reposition Recovery
4. `frontend/components/autonomous/DeviationHeatmap.tsx` — Dropdown filter
5. `frontend/components/autonomous/DriverAttribution.tsx` — Major rework (L2 default, L3 drilldown, dropdown, legend, axis, confidence)
6. `frontend/components/autonomous/RecoveryTimeline.tsx` — L2/L3 dropdown, legend above
7. `frontend/components/autonomous/AnomalyTable.tsx` — Add vizNumber prop to title
8. `frontend/components/autonomous/VarianceHistogram.tsx` — Add vizNumber prop to title
9. `frontend/components/autonomous/CrossDeptSignalStrip.tsx` — Add vizNumber prop to title
10. `frontend/components/autonomous/StockoutRiskTable.tsx` — Add vizNumber prop to title


---

## User Context

I want the persona handoff section below each persona like the demandplanner should explain step 1 n 2 and then the supply planner 3 and 4 and then the director of demand planner with step 5 and at the end of each persona i want a handoff too to the next persona just do not want this ection in the report [Image #1] and this feature should be handled by the orvell header persona drill down itself as we already have create a new plan with this updated change and then lets decide