---
created: 2026-09-24T09:55:54.166Z
session: 3470b7d3-0a27-4b86-aa70-92d5d01c694e
working_directory: C:\Users\2000167629\Vibe-Analytics-Phase3
---


## Context

- **Section 5 COMMUNICATE** lives inside `ExecutiveBriefingPack.tsx` (163 lines)
- **Cross-Department table** = lines 62-118 (KPI banner + department table + HowToReadIt) — must stay unchanged
- **Cross-Department Contentions** = lines 120-155 (amber cards showing competing departments/resources) — this gets **replaced** with the Top 5 recommendation cards
- **RecommendationCards component** already has all the card logic (L3+Region, approval, sorting, top 5) — just needs a `mode` prop to switch between "execution" (Section 4) and "communication" (Section 5) framing
- Both sections use `analytics.recommendations` from the same API response — same data, same component

## Implementation Steps (3 files)

### Step 1: `RecommendationCards.tsx` — Add `mode` prop
- Add optional `mode?: "execution" | "communication"` prop (default: `"execution"`)
- When `mode === "communication"`:
  - Insight uses leadership/communication language ("decisions required", "communication priorities") instead of execution language
- The card content, sort order, approval logic, and data remain identical regardless of mode

### Step 2: `ExecutiveBriefingPack.tsx` — Replace Contentions with RecommendationCards
- Add `recommendations` prop (`RecommendationCard[]`)
- Import `RecommendationCards` component
- Replace the Cross-Department Contentions block (lines 120-155) with:
  - Communication-focused heading: "Priority Recommendations for Leadership Review"
  - Descriptive paragraph explaining the L3+Region cards
  - `<RecommendationCards data={recommendations} mode="communication" />`
- Keep the Cross-Department table and KPI banner completely unchanged
- Keep the existing `narrative` ChartExplainer at the bottom

### Step 3: `page.tsx` — Pass `analytics.recommendations` to ExecutiveBriefingPack
- Add `recommendations={analytics?.recommendations ?? []}` prop to the `<ExecutiveBriefingPack>` usage in Section 5

## Verification
1. `npx tsc --noEmit` — zero errors
2. Section 4 ACT: still shows 5 cards, L3+Region, approval badges — unchanged
3. Section 5 COMMUNICATE: Cross-Department table untouched, Contentions replaced by same 5 cards
4. Cards in both sections show identical values (same data source)
5. Section 5 heading says "Priority Recommendations for Leadership Review", not "ACT"
6. Section 5 insight uses communication language

## Critical Files
- `frontend/components/autonomous/RecommendationCards.tsx` — Add mode prop for execution vs communication framing
- `frontend/components/autonomous/ExecutiveBriefingPack.tsx` — Replace Contentions with RecommendationCards
- `frontend/app/autonomous/page.tsx` — Pass recommendations prop to ExecutiveBriefingPack
