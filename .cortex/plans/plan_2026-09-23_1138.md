---
created: 2026-09-23T11:38:51.493Z
session: 0a688ab8-dd2a-453d-a7e2-8969a3a87c82
working_directory: C:\Users\2000167629\Vibe-Analytics-Phase3
---


## Plan: Root Cause Driver Attribution — Align with StockoutRiskTable Pattern

### What Already Works (from earlier rewrite)
- `normalizeDriver()` function with correct mappings
- `DRIVER_COLORS` map with all 5 standardized names
- `aggregateToL2()` for default view
- `deduplicateL3()` for L3 view
- Cascading L2/L3 dropdowns with reset behavior
- Dynamic `scopedInsight` with driver-specific action recommendations

### Changes Needed (single file: `DriverAttribution.tsx`)

1. **Fix L3 dropdown label**: "Product:" → "Subcategory:" and "All Products in {L2}" → "All Subcategories in {L2}" (per requirement)

2. **Fix drilldown indicator text**: "Showing L3 products in {L2}" → "Showing subcategories in {L2}"

3. **Fix Y-axis label**: "Products in {L2}" → "Subcategories in {L2}"

4. **Add Residual explanation to HOW_TO_READ**: Add the required bullet about Residual representing unexplained attribution

5. **Enhance normalizeDriver()**: Add the additional aliases specified (Weather Signal, Promotional Activity, Competitive Activity, Social Signal, Unexplained, etc.)

6. **Fix driver trace ordering**: Force consistent legend order (Weather → Promotions → Competitor Actions → Digital Signals → Residual) instead of data-dependent order

7. **Enhance Cortex AI Insight for Residual**: Add Residual-dominant and Residual-high-but-not-dominant narrative paths per the detailed requirements (missing from current implementation)

8. **Enhance Cortex AI Insight narratives**: Expand each driver-dominant path with the richer detail specified (weather temporary/seasonal/event-driven, promotions incremental vs pulled-forward, competitor temporary vs structural, digital persistence and decay, residual missing variables/model uncertainty)

9. **Ensure drivers not present in filtered data are not fabricated**: The current `find()` already returns 0 for missing drivers in traces — verify this doesn't create phantom legend entries; filter traces to only drivers actually present in the filtered data

No changes needed to `route.ts`, `orchestration-types.ts`, or `page.tsx` — only `DriverAttribution.tsx`.
