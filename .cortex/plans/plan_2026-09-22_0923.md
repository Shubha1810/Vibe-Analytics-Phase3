---
created: 2026-09-22T09:23:06.809Z
session: a6e85e68-5418-4492-9582-5af3630f9fdd
working_directory: C:\Users\2000167629\Vibe-Analytics-Phase3
---


## Plan: Fix Insight Contradictions + Header Spacing

### 1. Root Cause Fix — Data-Driven Insights for All Components

Replace the static narratives from `buildPerVizNarratives()` with computed insights inside each component that uses live analytics data:

**Components to update** (ones that currently use static narratives):
- **VarianceHistogram** — compute insight from `data` (VarianceBucket[]) using actual zone counts, mean, std dev
- **AnomalyTable** — compute insight from `data` (AnomalyRow[]) using actual severity counts, value-at-risk totals
- **CrossDeptSignalStrip** — compute insight from `anomalies` data
- **StockoutRiskTable** — compute insight from `data` (AnomalyRow[]) using actual days-to-impact, risk counts

Each will get a `useMemo` that builds a narrative string with `|IMPLICATIONS|` and `|ACTIONS|` delimiters from the actual displayed data. The `narrative` prop from `buildPerVizNarratives()` becomes a fallback only.

**Already correct** (have dynamic `scopedInsight`):
- DeviationHeatmap ✓
- DriverAttribution ✓

### 2. Header Spacing Fix

Reduce `gap-2` to `gap-1` in the shared h3 title pattern across all visualization components. This applies globally to the `text-base font-bold mb-3 flex items-center gap-2` class.

**Files**: AnomalyTable, VarianceHistogram, CrossDeptSignalStrip, StockoutRiskTable, DeviationHeatmap, DriverAttribution, RecoveryTimeline (7 files)

### 3. Keep `buildPerVizNarratives()` as fallback
Don't delete it — it's still useful when analytics data hasn't loaded yet. But each component will prefer its own computed insight over the prop.

### Files Modified (7 component files + page.tsx stays unchanged)
