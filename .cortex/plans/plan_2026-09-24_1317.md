---
created: 2026-09-24T13:17:26.930Z
session: 3470b7d3-0a27-4b86-aa70-92d5d01c694e
working_directory: C:\Users\2000167629\Vibe-Analytics-Phase3
---


## Context & Key Findings

### Issue 1: Heatmap deviation >100%
`WTD_DEMAND_DEVIATION_PCT` is a **pre-computed aggregate metric** from the data pipeline, not a simple `(actual-expected)/expected` percentage. It can legitimately exceed 100% — for example, Smart TVs in South-Central shows 696% while the simple per-SKU ratio is 95%. This appears to be a **cumulative or weighted deviation across SKUs** in each L3+Region combo. The heatmap displays this field correctly. **No code fix needed** — but the heatmap tooltip/legend should clarify this is "weighted demand deviation" not a bounded percentage.

### Issue 2: Recovery Trajectory "3470%" peak deviation
The Recovery Timeline component's insight says "Peak deviation projected at 3470%". This comes from the `computedInsight` in `RecoveryTimeline.tsx` which likely computes a percentage change from recovery curve data. This is a **frontend calculation issue** in the insight generation — the peak deviation formula is producing unbounded values. Need to investigate and fix the insight calculation.

### Issue 3: Category Action Matrix $0 values
Dairy and Fresh Produce show $0 for Day 0/3/7 because the recovery data for those L2+Region combos at those specific day horizons is genuinely $0 or absent (the recovery window may have passed). User chose: **show $0 as-is with explanation**. Add a note/tooltip.

### Issue 4: Portfolio Health outliers
5 L3+Region combos have deviation >200% (up to 696%). User chose **cap at 250%** — exclude the 4 combos above 250% from the histogram so the main distribution is visible.

## Implementation Steps (3 files)

### Step 1: `VarianceHistogram.tsx` — Cap outliers at 250% deviation
- Filter out data points where `deviation_bucket > 250` before rendering the histogram
- The excluded outliers should NOT affect the zone counts (Normal/Watch/Anomaly) — compute zones from full data, then filter for chart display only
- Add a note below the chart: "N outlier combinations (>250% deviation) excluded from chart for readability"

### Step 2: `RecoveryTimeline.tsx` — Fix "3470%" insight + add $0 explanation
- Investigate the `computedInsight` calculation that produces the unbounded peak deviation percentage
- Cap or fix the peak deviation calculation to use a sensible metric
- Add a small note to the Category Action Matrix explaining that $0 values indicate the recovery window has passed or the value is fully eroded at that time horizon

### Step 3: `DeviationHeatmap.tsx` — Add tooltip clarification
- Update the heatmap tooltip or subtitle to clarify that "Dev %" represents "Weighted Demand Deviation" which can exceed 100% when multiple SKUs in a category-region show compounding deviation

## Verification
1. TypeScript check — zero errors
2. Portfolio Health histogram X-axis stops around 200-250%, main distribution visible
3. Excluded outlier count shown below chart
4. Zone counts (Normal/Watch/Anomaly) still use full data
5. Recovery insight doesn't show unbounded percentages
6. $0 values have explanation in action matrix
7. Heatmap clarifies the deviation metric

## Critical Files
- `frontend/components/autonomous/VarianceHistogram.tsx` — Cap outliers at 250% for chart display
- `frontend/components/autonomous/RecoveryTimeline.tsx` — Fix insight peak deviation calculation + $0 explanation
- `frontend/components/autonomous/DeviationHeatmap.tsx` — Clarify deviation metric in tooltip/subtitle


---

## User Context

Make sure you do these cahnges without messing anything up