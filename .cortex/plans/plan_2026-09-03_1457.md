---
created: 2026-09-03T14:57:37.385Z
session: b95bd014-a5e9-47b3-bb8c-23be4839a6dd
working_directory: C:\Users\2000167629\Vibe-Analytics-Phase3
---

## Task A: Rewrite CLASSIFY_DEMAND_SENSING_INTENT UDF

**Problems identified** (4 of 6 golden prompts misclassify):
- No DETECT intent for "what changed", "where is risk concentrated"
- No BENCHMARK intent for "is that abnormal or seasonal", "benchmark against last year"  
- No RECOVERY_ECONOMICS intent for "how much can I capture", "cost of delay"
- "changed" in `trend_signals` hijacks descriptive questions
- "rank" in `comparison_signals` hijacks recommendation questions
- "forecast error" in `forecast_accuracy_signals` hijacks prediction questions
- Confidence penalty of -0.30 is too aggressive for context-dependent prompts

**Changes:**
1. Add 3 new intents: `DETECT`, `BENCHMARK`, `RECOVERY_ECONOMICS`
2. Restructure priority order: driver → recovery → scenario → forecast_accuracy → lost_sales → root_cause → **detect** → **benchmark** → trend → comparison → prediction → recommendation → perishable → replenishment → risk → kpi
3. Move "changed", "what happened", "anomalies", "exposure", "concentrated" into `detect_signals`
4. Add "benchmark", "seasonal", "abnormal", "yoy", "same week last year" to `benchmark_signals`
5. Add "already safe", "capture today", "delay cost", "recoverable", "window" to `recovery_signals`
6. Move "rank" + "intervention"/"option" combo to RECOMMENDATION (not COMPARISON)
7. Reduce confidence penalty from -0.30 to -0.15 for context-dependent prompts (references like "that heatwave", "those categories")
8. Add DETECT, BENCHMARK, RECOVERY_ECONOMICS to chart/rationale/subtask maps
9. Ensure prompt 5 ("that heatwave") gets lower confidence (~0.70) to trigger HITL since it depends on prior context

**Expected results after fix:**
| # | Prompt | Intent | Confidence | HITL? |
|---|--------|--------|-----------|-------|
| 1 | Risk concentrated this week | DETECT | 0.85 | No |
| 2 | Benchmark against last year | BENCHMARK | 0.85 | No |
| 3 | Decompose drivers | DEMAND_DRIVER_ATTRIBUTION | 0.85 | No |
| 4 | Project 13 weeks out | PREDICTION | 0.85 | No |
| 5 | That heatwave, capture today | RECOVERY_ECONOMICS | 0.70 | **Yes** (below 0.75 threshold) |
| 6 | Rank interventions, suppliers | RECOMMENDATION | 0.80 | No |

## Task B: Update PLAN_SKELETON procedure

**Changes:**
1. Add DETECT, BENCHMARK, RECOVERY_ECONOMICS to the `CANONICAL` set
2. Add per-intent HITL thresholds for new intents
3. Update normalisation mapping (DEMAND_DRIVER_ATTRIBUTION → ROOT_CAUSE stays; add new intent pass-throughs)
4. Add misclassification heuristics for the new intents
5. Keep the "prior not verdict" design — PLAN_SKELETON still calls classify_intent and wraps it

## Files to modify:
1. `CLASSIFY_DEMAND_SENSING_INTENT` UDF — full rewrite via `CREATE OR REPLACE FUNCTION`
2. `PLAN_SKELETON` procedure — update via `CREATE OR REPLACE PROCEDURE`
3. Test both against all 6 golden prompts

---

## User Context

For both tasks A and B Intent is to have the confidence score below the threshold and trigger HITL workflow at either prompt 2 or 3 and not prompt 5. We also built Fuzzylogic to robustly handle typos and paraphrasing- ensure that this is part of the custom tool. Why is Plan_Skeleton calling Classify_intent when it is supposed to do the job itself- Unless there is a legitimate reason lets replicate classify_intent to replace Plan_skeleton instead of editing it in the master_orchestrator_agent. Answer these and generate a updated plan