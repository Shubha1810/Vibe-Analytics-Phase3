---
created: 2026-09-04T11:30:08.114Z
session: bc1fca1d-b8a3-4ef0-b422-ddc8a95b7e3b
working_directory: C:\Users\2000167629\Vibe-Analytics-Phase3
---


## Plan: End-to-End VQ Enhancement Demo

### Root Cause Fix
`SP_GENERATE_REMEDIATION` never reads `FEEDBACK_COMMENT` from `FEEDBACK_MASTER_TABLE`. The AI prompt only sees the query and failed SQL — it never sees **what the user said was wrong**. This is why the fix quality was weak.

### Step 1: Fix SP_GENERATE_REMEDIATION
- Add `FEEDBACK_COMMENT` to the SELECT from `FEEDBACK_MASTER_TABLE`
- Inject it into the AI prompt as: `USER FEEDBACK: <comment>` — placed prominently so the AI must address it
- Add instruction: "You MUST address every specific correction mentioned in the user feedback"

### Step 2: Demo Flow (you do this in the app)

**Round 1 — Break it:**
1. Ask: **"What is the forecast accuracy by region?"** (matches VQ 03, similarity ~0.99)
2. Give thumbs-down → "Inaccurate data" → Comment: *"MAPE should be calculated as AVG(ABS_PCT_ERROR) not AVG(ABS(ACTUAL-FORECAST)/ACTUAL). Also must filter FORECAST_TYPE = 'actuals_vs_forecast'. Group by REGION only, not by department."*
3. Verify: check `FEEDBACK_RULES` — rule should contain corrected SQL with the exact logic from your comment

**Round 2 — Verify injection:**
4. Re-ask: **"What is the forecast accuracy by region?"**
5. Check backend console: should show `[Feedback Rules] Injected 1 rules`
6. Response should use the corrected aggregation

**Round 3 — Revert to original:**
7. Give thumbs-down again → "Inaccurate data" → Comment: *"Actually the MAPE calculation should include department breakdown too. Use AVG(ABS_PCT_ERROR) grouped by REGION and DEPARTMENT with FORECAST_TYPE = 'actuals_vs_forecast'."*
8. Old rule gets superseded by new rule (both active, but newer has higher similarity)
9. Re-ask same question — now uses the reverted/corrected logic

### Step 3: Verification queries
- After each round, run verification SQL to show triage → remediation → rule chain
- Show HIT_COUNT incrementing on matched rules

### Files Modified
- **Snowflake**: `SP_GENERATE_REMEDIATION` (add FEEDBACK_COMMENT to prompt)
- No frontend/backend changes needed (already done)


---

## User Context

Plan this robustly without messing anything up and give me steps to do this