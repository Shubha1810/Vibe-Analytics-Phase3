---
created: 2026-09-16T05:38:09.007Z
session: f14b6b2d-494d-46df-9e5f-c80b5daa2e12
working_directory: C:\Users\2000167629\Vibe-Analytics-Phase3
---

## Two Changes

### Change 1: Remove persona picker from autonomous page
- Remove the persona multi-select checkboxes and the idle state from `frontend/app/autonomous/page.tsx`
- When "Run Pipeline" is clicked, read the single persona from the header dropdown (via `useApp().persona`) and submit it to `SUBMIT_ORCHESTRATION`
- The autonomous page should show the gradient header banner + "Run Pipeline" button immediately (no persona picker screen)

### Change 2: Replace the basic network visualization with the Phase 2 neon SVG DAG
- Port the `AgentNetworkGraph` component from `vibe-analytics-react/frontend/app/autonomous/page.tsx` (lines 345-633)
- Adapt it for the new 9-node topology instead of 14 nodes:
  - **GRAPH_NODES**: master_plan, data_gathering, task_trend, task_dimensional, task_root_cause, task_predictive, prescriptive, validation_gate, exec_report (positioned in a similar multi-row layout)
  - **GRAPH_EDGES**: master_plan→data_gathering, data_gathering→4 parallel tasks, 4 tasks→prescriptive, prescriptive→validation_gate, validation_gate→exec_report
- Keep the same dark background, neon glow effects (cyan/green/red), animated edge flow, traveling dots, pulsing borders, monospace font styling
- Map the polling status values (`RUNNING`, `ok`, `error`, `timeout`, `degraded`) to the visual states (running=cyan glow, ok=green, error=red, timeout=orange, degraded=yellow)
- Keep the gradient header banner with "Autonomous Mode" title, decorative snowflake SVGs, running status badge
- Keep the "Run Pipeline" / "Reset" button row below the header

### Files to modify
- `frontend/app/autonomous/page.tsx` — rewrite the running state visualization + remove idle persona picker
