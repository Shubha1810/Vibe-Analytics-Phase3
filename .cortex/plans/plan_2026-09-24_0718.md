---
created: 2026-09-24T07:18:18.392Z
session: 3470b7d3-0a27-4b86-aa70-92d5d01c694e
working_directory: C:\Users\2000167629\Vibe-Analytics-Phase3
---


## Context

**Data findings:**
- Section 3.2 "duplicates" are actually **different RISK_IDs** with different `RISK_TYPE` (Perishable Freshness Exposure, Unmet Demand Surge, Forecast Miss, Stockout Exposure). Same L3+Region but different risk windows. User chose: **keep highest Units at Risk per L3+Region**.
- "National" is a real region (1 of 7). User said keep it everywhere.
- Section 4 currently uses L2+Region (`fetchRecommendations` GROUP BY `p.CATEGORY_L2, r.REGION`). Must change to L3+Region.
- L3+Region data confirmed: e.g., Milk+South-Central = $4.7M impact, $300K cost, 87% confidence.
- No existing approval threshold in the codebase — need to create one.

**Critical files explored:**
- `BrightwayIntro.tsx` — lines 181-200 contain the "7-14 days" statement to remove
- `StockoutRiskTable.tsx` — current dedup is by `risk_id` only; needs L3+Region dedup (highest units_at_risk)
- `RecommendationCards.tsx` — currently L2+Region, needs L3+Region + approval logic + top 5
- `route.ts` — `fetchStockout` (line 191) and `fetchRecommendations` (line 216) need query changes
- `orchestration-types.ts` — `RecommendationCard` needs `category_l3` and `total_value_at_risk` fields

---

## Implementation Steps

### Step 1: BrightwayIntro.tsx — Remove "7-14 days" statement
- Delete lines 181-200 (the entire `{/* Value prop */}` div containing the statement)
- No replacement, no empty gap

### Step 2: route.ts — Fix `fetchStockout` query for L3+Region dedup
- Change the QUALIFY clause from `PARTITION BY r.RISK_ID` to `PARTITION BY p.CATEGORY_L2, r.CATEGORY_L3, r.REGION ORDER BY r.UNITS_AT_RISK DESC`
- This keeps only the **highest Units at Risk** row per L3+Region combo, eliminating the "duplicate-looking" rows

### Step 3: StockoutRiskTable.tsx — Fix sort order and dedup
- Change `sortRows()` to sort by: **Units at Risk DESC** → **Revenue at Risk DESC** → Stockout Rate DESC → Severity rank → risk_id tiebreaker
- Change frontend dedup from `risk_id`-only to `category_l2 + category + region` key (keep highest `units_at_risk`), as a safety net for the SQL dedup
- Update `howToReadBullets` to say "Rows are sorted by Units at Risk (highest first)"
- Update `computedInsight` to lead with highest Units at Risk instead of highest Stockout Rate

### Step 4: orchestration-types.ts — Update `RecommendationCard` type
- Add `category_l3: string` field
- Add `total_value_at_risk: number` field
- Keep `category_l2` as parent context

### Step 5: route.ts — Change `fetchRecommendations` to L3+Region grain
- Change GROUP BY from `p.CATEGORY_L2, r.REGION` to `r.CATEGORY_L3, p.CATEGORY_L2, r.REGION`
- Add `r.CATEGORY_L3` to SELECT
- Add `SUM(c.TOTAL_VALUE_AT_RISK_USD) as TOTAL_VAR` to SELECT
- Map new fields in the return: `category_l3`, `total_value_at_risk`

### Step 6: RecommendationCards.tsx — Full rewrite for L3+Region + approval
- **Top 5** (not 10): `.slice(0, 5)`
- **Sort by Impact only** + stable tiebreaker (category_l3 + region)
- **Approval threshold**: `const APPROVAL_COST_THRESHOLD = 250_000;` as a single shared constant
- **Authority logic**: `cost_usd > APPROVAL_COST_THRESHOLD ? "Approval Required" : "Direct Action"`
- **Card title**: `{category_l3} — {region}` (L3 is primary scope)
- **Card Scope section**: Parent Category (L2), Subcategory (L3), Region
- **Findings bullet**: references L3+Region, not L2
- **Approval section**: new bullet group showing Cost, authority status, badge (green for Direct Action, amber for Approval Required), next step guidance
- **Metadata footer**: add Parent Category (L2), Subcategory (L3), Region, Impact, Total Value at Risk, Cost, Confidence, Authority
- **Cortex AI Insight**: generated from top 5, references L3+Region, identifies which are Direct Action vs Approval Required, mentions threshold

### Step 7: Clear `.next` cache
- Delete `.next` folder to force recompilation

---

## Verification
1. `npx tsc --noEmit` — zero errors
2. Verify BrightwayIntro no longer has "7-14 days" text
3. Verify Section 3.2: no duplicate L3+Region rows, sorted by Units at Risk DESC
4. Verify Section 4: cards show L3+Region (e.g., "Milk — South-Central"), not L2+Region
5. Verify Section 4: only 5 cards displayed
6. Verify Section 4: Cost shows real values (e.g., $300K), not $0
7. Verify Section 4: authority badge shows "Direct Action" or "Approval Required" based on $250K threshold

## Critical Files
- `frontend/components/autonomous/BrightwayIntro.tsx` — Remove value-prop statement
- `frontend/app/api/autonomous/analytics/route.ts` — Fix both fetchStockout and fetchRecommendations queries
- `frontend/components/autonomous/StockoutRiskTable.tsx` — Fix dedup + sort order
- `frontend/components/autonomous/RecommendationCards.tsx` — L3+Region rewrite with approval logic
- `frontend/lib/orchestration-types.ts` — Add category_l3 and total_value_at_risk to RecommendationCard
