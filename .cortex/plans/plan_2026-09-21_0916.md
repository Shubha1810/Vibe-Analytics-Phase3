---
created: 2026-09-21T09:16:38.288Z
session: c913ad09-c2ca-4c8d-96dd-c57820609caf
working_directory: C:\Users\2000167629\Vibe-Analytics-Phase3
---

## Plan: Enriched Insights, Dynamic Filters, Spacing & ACT Section

### 1. ChartExplainer.tsx — Fix markdown bold + add Business Implications / Recommended Actions
- Add `**text**` markdown bold parsing to `boldMetrics()` function
- Add two new mandatory sections to the render: "Business Implications" and "Recommended Actions"
- These are parsed from the narrative text using delimiter markers (e.g., lines starting with `[IMPLICATIONS]` and `[ACTIONS]`) or extracted from structured input

### 2. buildPerVizNarratives() in page.tsx — Add implications + actions to every narrative
- Extend each per-viz narrative to include `[IMPLICATIONS] ...` and `[ACTIONS] ...` delimited sections
- Each visualization gets contextually appropriate business implications and recommended actions derived from the exec_report data

### 3. DeviationHeatmap.tsx — Fix scoped insight formatting
- Add Business Implications and Recommended Actions to the `scopedInsight` computation
- Already dynamic (updates on filter change) — just needs richer content

### 4. DriverAttribution.tsx — Add dynamic insight based on department filter
- Compute a `scopedInsight` from `filtered` data (like DeviationHeatmap pattern)
- Pass the dynamic insight to ChartExplainer instead of the static narrative
- Includes implications and actions that change with the filter

### 5. page.tsx — Spacing fix
- Add `mb-8` wrapper divs around each visualization component in the Demand Planner section (and all personas)
- Ensures clear visual separation between insight card and next section title

### 6. page.tsx — ACT section insight card
- Add a ChartExplainer to the Supply Planner ACT section with a dedicated narrative from `vizNarr`
- Add a new `act` key to `PerVizNarratives` for the ACT section narrative

### Files Modified (4)
1. `ChartExplainer.tsx` — markdown bold + new sections
2. `page.tsx` — enriched narratives, spacing, ACT insight
3. `DeviationHeatmap.tsx` — enriched scoped insight
4. `DriverAttribution.tsx` — dynamic filter insight