---
created: 2026-09-11T10:37:07.380Z
session: 8052e343-c57d-4d3d-b3b9-fda71866a300
working_directory: C:\Users\2000167629\Vibe-Analytics-Phase3
---

## Autonomous Module Integration — Implementation Plan

### Step 1: Foundation (s31f1) — no dependencies
- Create `frontend/lib/orchestration-types.ts` — TypeScript types for all orchestration payloads (OrchestrationResult, OrchestrationEvent, ExecReport, Section, etc.) derived from real `run-899c53188864` payload
- Create `frontend/lib/snowflake-api.ts` — server-side utility calling Snowflake SQL API (`POST /api/v2/statements`) with PAT auth, async polling, VARIANT parsing
- Update `frontend/lib/constants.ts` — replace "Demand Analyst" with "Director of Demand Planning" + "Supply Chain Director"

### Step 2: API Routes (s8d7e) — depends on Step 1
- Create 3 Next.js API routes: `submit/route.ts`, `events/route.ts`, `result/route.ts` — each calling the corresponding stored procedure via snowflake-api helper
- Delete old Flask proxy stubs (`agent-step/route.ts`, `run-pipeline/route.ts`)
- Update `frontend/lib/api.ts` — add `submitOrchestration`, `getOrchestrationEvents`, `getOrchestrationResult`; remove dead old methods

### Step 3: AppContext (s24fd) — depends on Step 1
- Replace old pipeline state (PipelineResult, ScorecardData) with new orchestration state (runId, orchestrationStatus, nodeEvents, orchestrationResult, selectedPersonas, error)

### Step 4: Autonomous Page (sbd11) — depends on Steps 2 + 3
- Create `frontend/app/autonomous/page.tsx` with 3 states:
  - **Idle**: persona picker + Run Pipeline button
  - **Running**: live agent network DAG visualization polling events every 2-3s with node status coloring
  - **Completed**: executive briefing report (enterprise summary, per-persona sections, validation, caveats)
- Handle all 5 required edge cases (DATA GAP, null exec_report, FAILED, node errors, timing)

### Step 5: Verify (sec47) — depends on Step 4
- TypeScript compilation check, Next.js build, import verification, edge case review

**3 implementors + 1 verifier = 4 agents total**