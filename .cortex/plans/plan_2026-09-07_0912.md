---
created: 2026-09-07T09:12:26.594Z
session: 37bc853b-0be9-441f-b01c-2185fdc99252
working_directory: C:\Users\2000167629\Vibe-Analytics-Phase3
---


# Autonomous Module Agent Network — Implementation Plan

## Summary
Create 9 Cortex Agents (`*_AUTO_DEMANDSENSING`) in `DEMANDSENSING_AI.DEMANDSENSING_SCHEMA` using `ALTER AGENT ... MODIFY LIVE VERSION SET SPECIFICATION`. Each mirrors the interactive counterpart but is redesigned for **unattended, multi-persona execution** with no user question, informed by the storyboard's Morning Signal Pack (DETECT) and Executive Briefing Pack (COMMUNICATE).

---

## Phase 0: Pre-Build Audit (before any agent creation)

### 0.1 — Read exact specs of all 6 interactive counterparts
Run `DESCRIBE AGENT` on each of the 6 interactive agents we're cloning from:
- `DIMENSIONAL_ANALYSIS_AGENT_DEMANDSENSING`
- `ROOT_CAUSE_AGENT_DEMANDSENSING`
- `PREDICTIVE_AGENT_DEMANDSENSING`
- `PRESCRIPTIVE_AGENT_DEMANDSENSING`
- `VALIDATION_AGENT_DEMANDSENSING`
- `INSIGHTS_NARRATION_AGENT_DEMANDSENSING`

(TREND, DATA_GATHERING, and MASTER already read.)

### 0.2 — Audit TIER_C / TIER_D for autonomous compatibility
Read the procedure bodies of `RUN_TIER_C_CHECK` and `TIER_D_GATE` to verify they handle `NULL` questions. Report findings before proceeding.

### 0.3 — Confirm PLAN_SKELETON absence
Already confirmed — does not exist. `CLASSIFY_USER_INTENT` is the real tool.

---

## Phase 1: Wave-2 Task Agents (parallel analytical agents)

All four share the same tool/skill template (no DB access, artifact-only). Each gets PythonSandbox + GET_SLICE + FETCH_ARTIFACT + LIST_ARTIFACTS.

### 1.1 — TREND_DISCOVERY_AGENT_AUTO_DEMANDSENSING
- **Model**: `claude-haiku-4-5` | **Budget**: 60s / 20,000 tokens
- **Skills**: `descriptive_demand_analysis`, `artifact_scratchpad_protocol`, `canonical_data_facts`
- **Storyboard grounding**: Morning Signal Pack — detect anomalies ("+28% Fresh Produce above forecast for 4th straight day"), identify patterns across 7-day windows, flag revenue-at-risk figures, temporal trend classification
- **Output**: JSON with `anomalies_detected[]`, `trend_patterns[]`, `yoy_comparison`, `narrative_summary`
- **Key difference from interactive**: No user question to answer — scans ALL artifacts for ALL personas in the run, surfaces anomalies ranked by business impact

### 1.2 — DIMENSIONAL_ANALYSIS_AGENT_AUTO_DEMANDSENSING
- **Model**: `claude-haiku-4-5` | **Budget**: 60s / 20,000 tokens
- **Skills**: `descriptive_demand_analysis`, `artifact_scratchpad_protocol`, `canonical_data_facts`
- **Storyboard grounding**: Morning Signal Pack — break down anomalies by region/store/sub-category ("Southeast & South-Central", "12-14 of 16 stores"), identify geographic concentration, cross-departmental awareness flags
- **Output**: JSON with `dimensional_breaks[]`, `concentration_analysis`, `cross_department_flags[]`, `narrative_summary`

### 1.3 — ROOT_CAUSE_AGENT_AUTO_DEMANDSENSING
- **Model**: `claude-haiku-4-5` | **Budget**: 60s / 20,000 tokens
- **Skills**: `descriptive_demand_analysis`, `artifact_scratchpad_protocol`, `canonical_data_facts`, `risk_recovery_economics`
- **Storyboard grounding**: Signal attribution from storyboard Step 2 — weather 45%, promotion 25%, competitor 15%, digital 10%, multicollinearity check (weather × digital r=0.62), dampening corrections, residual analysis
- **Output**: JSON with `attribution_drivers[]`, `multicollinearity_check`, `confidence_pct`, `residual_pp`, `narrative_summary`

### 1.4 — PREDICTIVE_AGENT_AUTO_DEMANDSENSING
- **Model**: `claude-haiku-4-5` | **Budget**: 60s / 20,000 tokens
- **Skills**: `predictive_prescriptive`, `ml_task_execution`, `artifact_scratchpad_protocol`, `canonical_data_facts`
- **Storyboard grounding**: Step 3 PREDICT — 14-day demand trajectory by driver, stockout risk by store-SKU, markdown risk for under-performers, decay curves ("spike holds ~5 more days then decays to +12%")
- **Output**: JSON with `trajectory_14d[]`, `stockout_risks[]`, `markdown_risks[]`, `opportunity_breakdown`, `narrative_summary`

### Test each agent
Run `SELECT SNOWFLAKE.CORTEX.DATA_AGENT_RUN(agent, prompt)` with an artifact-referencing prompt for each.

---

## Phase 2: Supporting Agents

### 2.1 — DATA_GATHERING_AGENT_AUTO_DEMANDSENSING
- **Model**: `claude-sonnet-4-5` | **Budget**: 120s / 40,000 tokens (higher than interactive — must scan multiple personas)
- **Tools**: Full data plane — Cortex Analyst (`DemandSensing_Analyst`), `VALIDATE_SQL_DRYRUN`, `PUT_SLICE`, `PUT_ARTIFACT`, `PythonSandbox`, `Web_Search`, MCP Server
- **Skills**: `data_preparation`, `artifact_scratchpad_protocol`, `canonical_data_facts`, `external_economic_context`
- **Storyboard grounding**: Morning Signal Pack generation — scan 180+ SKUs × 40 stores × 7 days per persona (Sarah/Mark/Emily), publish artifacts covering POS data, weather signals, competitor signals, promo calendars, digital trends, inventory/DOS, forecast baselines
- **Key difference from interactive**: Receives `data_requirements[]` from the master plan covering ALL 3 planners' portfolios. Publishes a comprehensive artifact set that wave-2 agents consume. No single-question focus.

### 2.2 — PRESCRIPTIVE_AGENT_AUTO_DEMANDSENSING
- **Model**: `claude-haiku-4-5` | **Budget**: 60s / 24,000 tokens
- **Skills**: `predictive_prescriptive`, `artifact_scratchpad_protocol`, `canonical_data_facts`, `risk_recovery_economics`
- **Storyboard grounding**: Step 4 ACT — specific replenishment orders ("4,400 units pre-cut salads, rush next-day"), expedite cost quantification ("$6K premium"), inventory rebalancing ("800 units same-day transfer"), demand-shaping ("pause BBQ promo in 3 stores"), markdown decisions ("30% cut on 400 units"), authority routing ("exceeds per-order threshold → supervisor"), net impact ("$142K of $148K at-risk protected")
- **Key difference from interactive**: ALWAYS runs (not conditional). Must produce recommended_actions for EVERY anomaly surfaced by wave-2, with impact, cost, and approval routing.
- **Output**: JSON with `recommended_actions[]` each containing `action`, `impact_usd`, `cost_usd`, `confidence`, `approval_required`, `authority_level`

### 2.3 — VALIDATION_AGENT_AUTO_DEMANDSENSING
- **Model**: `claude-haiku-4-5` | **Budget**: 60s / 20,000 tokens
- **Skills**: `response_quality_validation`, `demand_cross_validator`, `artifact_scratchpad_protocol`, `canonical_data_facts`
- **Key difference from interactive**: NEVER retries. A BLOCKED verdict becomes a caveat on the report. Checks citation integrity (every number must cite its artifact_name), cross-validates key figures, checks for truncation artifacts.
- **Output**: JSON with `verdict` (PASS/BLOCKED), `citation_check`, `cross_validation_results[]`, `caveats[]`

---

## Phase 3: Master Orchestrator

### 3.1 — MASTER_ORCHESTRATOR_AUTO_DEMANDSENSING
- **Model**: `claude-opus-4-6` | **Budget**: 60s / 20,000 tokens
- **Tool**: `CLASSIFY_USER_INTENT(VARCHAR, VARCHAR)` — called for metadata extraction (entity recognition, KPIs) but routing is FIXED (all wave-2 agents always run, prescriptive always, validation always, exec_report always)
- **Skills**: `master_orchestration`, `persona_context_scope`, `canonical_data_facts`
- **Input**: Synthesized directive (not a user question): "Autonomous executive reporting sweep..."
- **Output**: Same plan JSON shape: `{intent, confidence, route, agents, data_requirements, feature_requirements, artifacts_expected}`
- **Key differences from interactive**:
  - Schedulable agent list is 8 (the 4 wave-2 + data_gathering + prescriptive + validation + exec_report) — NOT the interactive 8
  - No 17-intent taxonomy routing — always "AUTONOMOUS_SWEEP"
  - No HITL gating — clarification requests suppressed
  - Must plan data_requirements covering ALL 3 planners' portfolios

---

## Phase 4: Executive Report Agent

### 4.1 — Propose exec report template (presented here for approval)

**Proposed JSON envelope matching the storyboard's Executive Briefing Pack:**

```json
{"report": {
  "as_of": "2026-07-14",
  "enterprise_summary": {
    "anomalies_total": 5,
    "anomalies_high_impact": 3,
    "departments_affected": 3,
    "net_revenue_at_stake_usd": 460000,
    "protected_recovered_usd": 272000,
    "total_action_cost_usd": 18000,
    "decisions_within_authority": 12,
    "decisions_pending_approval": 3,
    "cross_department_contentions": [
      {"resource": "expedited inbound capacity",
       "competing_departments": ["Fresh & Grocery", "Consumer Electronics"],
       "recommendation": "Fund both; if capped, salads first on return-per-dollar",
       "urgency": "48 hours"}
    ]
  },
  "sections": [
    {"persona": "Sarah Mitchell",
     "department": "Fresh & Grocery",
     "headline": "Heat-wave produce surge: $215K opportunity, $148K at stockout risk",
     "key_metrics": [
       {"metric": "Revenue at stake", "value": 215000, "delta": 0.28, "unit": "USD"},
       {"metric": "Stores at stockout risk", "value": 9, "delta": null, "unit": "count"}
     ],
     "anomalies": [
       {"anomaly": "Fresh Produce surge", "severity": "high", "regions": ["Southeast", "South-Central"], "deviation_pct": 28}
     ],
     "drivers": [
       {"driver": "Heat wave (+11°F)", "contribution_pct": 45, "confidence": "high"},
       {"driver": "Summer Fresh BBQ promo (2.1× plan)", "contribution_pct": 25, "confidence": "high"}
     ],
     "risks": ["9 stores stock out by Day 3 on pre-cut salads", "Artisan Breads: $42K markdown exposure"],
     "recommended_actions": [
       {"action": "Expedite 4,400 units pre-cut salads, next-day delivery", "impact_usd": 72000, "cost_usd": 6000, "confidence": 0.85, "approval_required": true, "authority": "Supply Planning Supervisor"}
     ],
     "trajectory": {"peak_day": 5, "decay_to_baseline_day": 14, "peak_deviation_pct": 28, "day7_deviation_pct": 12},
     "caveats": []}
  ],
  "pending_approvals": [
    {"item": "Salad expedite premium", "owner": "David → Supervisor", "amount_usd": 6000, "deadline": "Today"},
    {"item": "Berry sourcing split to Supplier E", "owner": "David → Procurement", "amount_usd": null, "deadline": "Today"},
    {"item": "Expedited freight budget (Fresh + CE)", "owner": "Lisa", "amount_usd": 18000, "deadline": "Before S&OP"}
  ],
  "validation_summary": {"verdict": "PASS", "caveats": []}
}}
```

### 4.2 — EXEC_REPORT_AGENT_AUTO_DEMANDSENSING
- **Model**: `claude-opus-4-6` | **Budget**: 90s / 40,000 tokens (must synthesize all wave outputs)
- **Skills**: `insight_communication`, `artifact_scratchpad_protocol`, `canonical_data_facts`, `persona_context_scope`
- **Tools**: GET_SLICE, FETCH_ARTIFACT, LIST_ARTIFACTS, PythonSandbox
- **Input**: Receives all wave-2 findings, prescriptive actions, and validation results via artifacts
- **Output**: The approved JSON envelope above
- **Storyboard grounding**: Step 5 COMMUNICATE — consolidates across departments, computes enterprise totals, identifies cross-department conflicts, routes approval items, produces S&OP-ready structure

---

## Phase 5: Testing

### 5.1 — Per-agent standalone tests via DATA_AGENT_RUN
Each agent tested individually with realistic prompts.

### 5.2 — End-to-end graph test
`run_graph_test.py --module autonomous` is **blocked** (files don't exist locally, SPCS not deployed). Report this as a known gap.

---

## Execution Order (from transfer prompt)
1. ~~Read TREND spec + artifact protocol + RUNTIME signatures~~ ✅ Done
2. **Propose exec report template** → included above, pending your approval
3. Create 4 wave-2 task agents → test each
4. Create data_gathering, prescriptive, validation
5. Create master → verify plan JSON
6. Create exec_report → verify envelope
7. ~~Run run_graph_test.py~~ → blocked (no local files, no SPCS)
8. Report: which agents exist, returns, gaps

---

## Open Items to Flag

1. **CLASSIFY_USER_INTENT in autonomous master**: Kept as-is per your direction. The function's 17-intent taxonomy and single-question design don't naturally fit the "no question, multi-persona sweep" autonomous input. The autonomous master will call it with a synthetic question but override the routing output. A purpose-built `CLASSIFY_AUTONOMOUS_SCOPE` function could be cleaner — flagging this for future consideration.

2. **stage2/ orchestrator files not local**: Cannot run `run_graph_test.py` or verify `modules.py` agent name expectations. Testing is limited to per-agent `DATA_AGENT_RUN` calls.

3. **SPCS service not deployed**: Full end-to-end testing via `SUBMIT_ORCHESTRATION` is blocked.

4. **TIER_C / TIER_D audit**: Will be done in Phase 0.2 before building validation agent.

5. **3 planners vs 5 personas**: Agents designed for Sarah/Mark/Emily portfolios. David (Supply) and Lisa (Director) are NOT separate scan targets — David's supply-side view is covered by prescriptive agent's recommendations, and Lisa's view is the exec_report itself. If David/Lisa ever need their own Morning Signal Packs, the data_gathering and wave-2 agents would need scope expansion.


---

## User Context

do it robustly and do not mess anything up which is already built and functional