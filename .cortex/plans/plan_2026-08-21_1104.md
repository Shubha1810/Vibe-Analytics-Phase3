---
created: 2026-08-21T11:04:17.808Z
session: bc1fca1d-b8a3-4ef0-b422-ddc8a95b7e3b
working_directory: C:\Users\2000167629\Vibe-Analytics-Phase3
---

## Context

**Current state:**
- `FeedbackBar.tsx` sends only 4 fields (query, response_text, feedback_type, persona) via `api.submitFeedback()`
- Backend `api_feedback()` at line 887 is a stub — returns success without persisting
- `FinalAnswer` has access to the full `AgentResponse` (planning, SQL, chart_config, result_set, suggested_queries) but doesn't pass it to FeedbackBar
- Target: `DEMANDSENSING_AI.DEMANDSENSING_SCHEMA.FEEDBACK_MASTER_TABLE`

## Implementation Steps

### 1. Create Snowflake table
Create `DEMANDSENSING_AI.DEMANDSENSING_SCHEMA.FEEDBACK_MASTER_TABLE` with columns for all 20+ fields from the architecture spec, including VARIANT columns for arrays/objects and a `CREATED_AT` timestamp defaulting to `CURRENT_TIMESTAMP()`.

### 2. Expand FeedbackBar component
- Add reason chips (5 positive, 6 negative) with a form that appears after thumb click
- Add optional comment textarea
- Accept the full `AgentResponse` as a prop (plus session_id, conversation_turn)
- Build the rich payload with all execution trace fields before sending

### 3. Update `api.ts` — `FeedbackPayload` interface and `submitFeedback`
- Define a `FeedbackPayload` interface with all 20+ fields
- Change `submitFeedback` to accept and POST the full payload object

### 4. Update `FinalAnswer.tsx` to pass `agentData` and context to FeedbackBar
- Pass `data` (AgentResponse), session_id, and conversation turn index as props

### 5. Implement backend `api_feedback()` endpoint
- Parse and validate the full payload
- Truncate fields to safe lengths
- Execute parameterized INSERT with `TRY_PARSE_JSON()` for VARIANT columns
- Return success/error

### 6. Wire session_id from AppContext
- Expose `currentChatId` to FeedbackBar as session_id; use message index as conversation_turn

## Verification
- Run the frontend build to confirm no TS errors
- Manually test thumbs up/down with reason + comment
- Query `SELECT * FROM DEMANDSENSING_AI.DEMANDSENSING_SCHEMA.FEEDBACK_MASTER_TABLE` to confirm rows land correctly

## Critical Files
- `frontend/components/chat/FeedbackBar.tsx` — Complete rewrite with reason chips + rich payload
- `frontend/lib/api.ts` — FeedbackPayload interface + updated submitFeedback
- `frontend/components/chat/FinalAnswer.tsx` — Pass additional props to FeedbackBar
- `backend/app.py` — Full endpoint implementation (lines ~887-889)
- Snowflake DDL — New table creation