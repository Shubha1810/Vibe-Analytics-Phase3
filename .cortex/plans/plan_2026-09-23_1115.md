---
created: 2026-09-23T11:15:08.879Z
session: 0a688ab8-dd2a-453d-a7e2-8969a3a87c82
working_directory: C:\Users\2000167629\Vibe-Analytics-Phase3
---


## Plan: Stockout & Availability Risk — L2/L3 Filter Dropdowns

### Data Findings
- **Source**: `FACT_DEMAND_RISK` joined with `DIM_PRODUCT` — 917 unique `RISK_ID`s, 14 L2, 42 L3, 7 regions
- **Dedup key**: `RISK_ID` (unique business record identifier)
- **Current issue**: `StockoutRiskTable` receives `AnomalyRow[]` from `fetchAnomalies` which **lacks** `STOCKOUT_RATE_PCT` and `RISK_ID`
- **Available fields**: `STOCKOUT_RATE_PCT`, `RISK_SEVERITY`, `RISK_ID`, `UNITS_AT_RISK`, `VALUE_AT_RISK_USD`, `DAYS_TO_IMPACT`, `PRIMARY_DRIVER`, `DETECTION_CONFIDENCE`
- **Sorting**: Stockout Rate DESC → Risk Severity (CRITICAL > HIGH > MEDIUM > LOW) → Value at Risk DESC → RISK_ID (deterministic tiebreaker)

### Changes (3 files)

#### 1. `orchestration-types.ts` — New `StockoutRow` interface
Add a dedicated type with: `risk_id`, `category_l2`, `category` (L3), `region`, `stockout_rate`, `severity`, `units_at_risk`, `value_at_risk`, `days_to_impact`, `primary_driver`, `confidence`. Add `stockout: StockoutRow[]` to `AnalyticsData`.

#### 2. `route.ts` — New `fetchStockout()` function
Dedicated query with `RISK_ID` for dedup, `STOCKOUT_RATE_PCT`, all needed fields. `QUALIFY ROW_NUMBER() OVER (PARTITION BY r.RISK_ID ORDER BY ...) = 1` for exact-duplicate removal at the SQL level. No LIMIT — filtering happens in the component. Add to `Promise.all` and include in `AnalyticsData` response.

#### 3. `StockoutRiskTable.tsx` — Rewrite with L2/L3 filters
- Accept new `StockoutRow[]` data prop
- Add `selectedL2` and `selectedL3` state
- L2 dropdown: "All Categories & Regions (L2)" default, all 14 L2s
- L3 dropdown: appears only when L2 selected, "All Subcategories in {L2}" default
- Filter rows by selected L2 → L3 (no aggregation)
- Dedup by `risk_id` in JS as safety net
- Sort: stockout_rate DESC → severity rank → value_at_risk DESC → risk_id
- Display top 10 rows with columns: Rank, Category (L2), Subcategory (L3), Region, Stockout Rate, Risk Level, Units at Risk, Revenue at Risk, Days to Impact, Primary Driver, Confidence
- View indicator badge
- Dynamic `computedInsight` from displayed 10 rows
- Reset L3 when L2 changes

#### 4. `page.tsx` — Update prop wiring
Pass `analytics.stockout` instead of `analytics.anomalies` to `StockoutRiskTable`.
