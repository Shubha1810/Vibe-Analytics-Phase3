---
created: 2026-09-21T08:58:46.300Z
session: c30f237f-1e57-44ce-b440-e4284320c70d
working_directory: C:\Users\2000167629\Vibe-Analytics-Phase3
---

## Plan: Unique Per-Visualization Insights, Titles, Unified Formatting & JSON Fix

### Current State
- **1 shared narrative** passed identically to all 8 visualization components
- **Only DeviationHeatmap** generates its own contextual insight from data
- **Missing titles** on AnomalyTable, DeviationHeatmap, VarianceHistogram, CrossDeptSignalStrip, DriverAttribution, RecoveryTimeline
- **3 components** (RecoveryTimeline, StockoutRiskTable, ExecutiveBriefingPack) render plain-text "AI Narrative" instead of using ChartExplainer
- **Cross-Department Contentions** renders raw JSON (`JSON.stringify(c)`) instead of structured content

### Changes (8 files)

#### 1. `page.tsx` — Map exec_report sections to individual visualizations
- Parse `sections[]` from exec_report and extract per-section narratives
- Build a `sectionNarratives` map: `{ anomaly_table: "...", heatmap: "...", variance: "...", ... }`
- Pass **unique narrative** to each component instead of the shared `formattedNarrative`
- Mapping logic: match section by `department`, `persona`, keyword in `headline`, or section index position

#### 2. `AnomalyTable.tsx` — Add title + unique insight
- Add `<h3>` title: "Multi-Signal Anomaly Detection"
- Use ChartExplainer with its own narrative (anomaly detection focus)

#### 3. `DeviationHeatmap.tsx` — Add title (insight already unique)
- Add `<h3>` title: "Portfolio Deviation Heatmap" 
- Already has its own scoped insight — no change needed there

#### 4. `VarianceHistogram.tsx` — Add title + unique insight
- Add `<h3>` title: "Portfolio Health Overview"
- Use ChartExplainer with variance-specific narrative

#### 5. `CrossDeptSignalStrip.tsx` — Add title + unique insight
- Add `<h3>` title: "Cross-Department Signal Awareness"
- Use ChartExplainer with cross-department narrative

#### 6. `DriverAttribution.tsx` — Add title + unique insight
- Add `<h3>` title: "Root Cause Driver Attribution"
- Use ChartExplainer with driver-specific narrative

#### 7. `RecoveryTimeline.tsx` — Add title + switch to ChartExplainer
- Add `<h3>` title: "14-Day Recovery Trajectory"
- Replace plain-text "AI Narrative" div with ChartExplainer component

#### 8. `StockoutRiskTable.tsx` — Switch to ChartExplainer
- Already has title ("Stockout & Availability Risk")
- Replace plain-text "AI Narrative" div with ChartExplainer

#### 9. `ExecutiveBriefingPack.tsx` — Fix JSON + switch to ChartExplainer
- **Fix Cross-Department Contentions**: Parse contention objects and render structured cards (competing_departments, recommendation, resource, urgency) instead of `JSON.stringify(c)`
- Replace plain-text "AI Narrative" div with ChartExplainer
- Title already implied by the component structure

#### 10. `ChartExplainer.tsx` — Enhance parsing robustness
- Handle both exec_report JSON sections and plain-text fallback
- Ensure consistent formatting across all consumers

### All 3 Personas Covered
- Demand Planner: AnomalyTable, DeviationHeatmap, VarianceHistogram, CrossDeptSignalStrip, DriverAttribution, RecoveryTimeline
- Supply Planner: StockoutRiskTable, ActionCards
- Director: ExecutiveBriefingPack, Cross-Department Contentions, ClosingTheLoop