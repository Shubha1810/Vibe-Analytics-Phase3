---
created: 2026-09-23T09:55:55.800Z
session: 0a688ab8-dd2a-453d-a7e2-8969a3a87c82
working_directory: C:\Users\2000167629\Vibe-Analytics-Phase3
---


## Plan: Rewrite DriverAttribution to Match AnomalyTable Hierarchy Pattern

### Key Findings
- **Driver names in DB**: `Weather`, `Promotion`, `Competitor`, `Digital`, `Residual` (5 drivers)
- **User expects**: `Weather`, `Promotions`, `Competitor Actions`, `Digital Signals` (4 drivers, different names)
- **Current color map** uses short names (`Promotion`, `Competitor`, `Digital`) — matches DB but not user expectations
- **`category` field** = L3 product names (Headphones, Laptops, Milk, etc.) — drilldown data is correct
- **`category_l2` field** = L2 category names (Audio, Computing, Dairy, etc.) — all 14 present
- All 14 L2 categories and 5 drivers present in the data (70 combinations total)

### Changes (single file: `DriverAttribution.tsx`)

1. **Driver name normalization** — Add a `normalizeDriver()` function:
   - `Promotion` / `Promo` → `Promotions`
   - `Competitor` → `Competitor Actions`
   - `Digital` → `Digital Signals`
   - `Weather` stays `Weather`
   - `Residual` stays `Residual` (excluded from display per user's 4-driver expectation, or kept as-is)

2. **Color map update** — Map to the normalized display names:
   - `Weather` → indigo
   - `Promotions` → amber
   - `Competitor Actions` → teal
   - `Digital Signals` → pink
   - `Residual` → slate

3. **Remove Department dropdown** — User explicitly said no Region and the focus should be category hierarchy only. Department filter adds unnecessary complexity; remove it.

4. **Add cascading L2 + L3 dropdowns** (matching AnomalyTable pattern):
   - **Dropdown 1**: Category (L2) — `All Categories (L2) — Top 10` default, lists all 14 L2s
   - **Dropdown 2**: Product (L3) — disabled/hidden when no L2 selected; dynamically populated from selected L2's products; `All Products in {L2}` default

5. **Three-level view logic**:
   - **No L2 selected**: Show top 10 L2 categories (aggregated via `aggregateToL2`)
   - **L2 selected, no L3**: Show all L3 products within that L2 (deduped by averaging per L3+driver)
   - **L2 + L3 selected**: Show single product's driver breakdown

6. **L3 drilldown indicator** — Add the same "Showing L3 products in {L2}" badge that AnomalyTable uses

7. **Update chart labels, title, insight** — Dynamic based on current selection level

8. **Normalize driver names in `aggregateToL2()` and L3 dedup** — Apply normalization before grouping to prevent duplicates from name variants

9. **Remove any Region references** — No region in filters, chart, aggregation keys, or insight narrative
