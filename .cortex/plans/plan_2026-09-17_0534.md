---
created: 2026-09-17T05:34:28.187Z
session: b8d30d1b-d633-4c80-917d-cbb2070bb657
working_directory: C:\Users\2000167629\Vibe-Analytics-Phase3
---

## Root Cause: Why Phase 3 AI Observability is Broken

**The observability page shows "No agents available" because of a persona key mismatch:**
- `constants.ts` defines personas as `"Director of Demand Planning"` and `"Supply Chain Director"`
- But the agent registries (both backend `OBSERVABILITY_AGENTS` and frontend `DEFAULT_AGENTS`) only have a key for `"Demand Analyst"`
- Since `agents[persona]` never matches, the dropdown is always empty

Additionally, the agent lists contain **6 non-existent agents** (no observability data in Snowflake): `BA_SUB_ORCHESTRATOR_DEMANDSENSING`, `DS_SUB_ORCHESTRATOR_DEMANDSENSING`, `FEATURE_ENHANCEMENT_AGENT_DEMANDSENSING`, `PERSONA_CONTEXT_AGENT_DEMANDSENSING`, `VALIDATION_AGENT_DEMANDSENSING`, `VISUALIZATION_AGENT_DEMANDSENSING`.

There's also a **schema typo** on line 22 of `page.tsx`: `schema: "DEMANDSENSING_AI"` should be `schema: "DEMANDSENSING_SCHEMA"`.

---

## Plan (3 files to update)

### 1. Update backend `OBSERVABILITY_AGENTS` in `backend/app.py` (~line 1190)
- Change persona key from `"Demand Analyst"` to match actual persona names (`"Director of Demand Planning"`, and optionally `"Supply Chain Director"`)
- **Interactive**: Keep only `INTERACTIVE_DEMANDSENSING_AGENT` (1 agent — the master interactive agent with all skills)
- **Autonomous**: Set to all 8 actual AUTO agents from `AGENT_RUN_METRICS`:
  - `MASTER_ORCHESTRATOR_AUTO_DEMANDSENSING`
  - `DATA_GATHERING_AGENT_AUTO_DEMANDSENSING`
  - `DIMENSIONAL_ANALYSIS_AGENT_AUTO_DEMANDSENSING`
  - `EXEC_REPORT_AGENT_AUTO_DEMANDSENSING`
  - `PREDICTIVE_AGENT_AUTO_DEMANDSENSING`
  - `PRESCRIPTIVE_AGENT_AUTO_DEMANDSENSING`
  - `ROOT_CAUSE_AGENT_AUTO_DEMANDSENSING`
  - `TREND_DISCOVERY_AGENT_AUTO_DEMANDSENSING`

### 2. Update frontend `DEFAULT_AGENTS` in `frontend/app/observability/page.tsx` (~line 19)
- Mirror the same changes: correct persona key, correct Interactive (1 agent), correct Autonomous (8 AUTO agents)
- Fix schema typo on the interactive agent entry

### 3. Verify the persona name used in `useApp()` matches the key
- Confirm `AppContext` uses persona values from `constants.ts` (`"Director of Demand Planning"`) so the lookup works end-to-end

No UI changes needed — the page structure already matches Phase 2.

---

## Clarification Question

You mentioned "Persona names will be changed" — should I use the current Phase 3 persona names (`"Director of Demand Planning"`, `"Supply Chain Director"`) as the keys for now, or are you about to change them to something else? If the latter, what will the new persona names be?

---

## User Context

Do it robustly