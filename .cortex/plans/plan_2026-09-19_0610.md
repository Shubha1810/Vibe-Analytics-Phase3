---
created: 2026-09-19T06:10:36.722Z
session: f14b6b2d-494d-46df-9e5f-c80b5daa2e12
working_directory: C:\Users\2000167629\Vibe-Analytics-Phase3
---


# DAG Visualization Fix Plan

All changes in **one file**: `frontend/app/autonomous/page.tsx` (the `AgentNetworkGraph` component)

## Fix 1: Single DAG + Run Counter (merge logic)
- Change the event merge to use "worst-case progression" per node across all 3 runs:
  - If **any** run has the node as `running` → show `running`
  - If **all** runs that reached this node show `ok` → show `ok`
  - If **any** run shows `error` → show `error`
  - Otherwise → `idle`
- Add a small counter badge on each node: "2/3" or "3/3" showing how many runs have completed that node
- This prevents the inconsistency where Prescriptive=running but Validation=completed (from different runs)

## Fix 2: Status Progression Consistency
- The merge logic above fixes this inherently — a node won't show "ok" until ALL runs that include it have completed it
- Exec Report won't show "running" until Validation is "ok" in the merged view

## Fix 3: Wave Nodes Restyle
- Keep `wave1_join` and `wave2_join` visible but:
  - Rename labels to "Sync Point" (or "Parallel Join")
  - Render as smaller, pill-shaped nodes (reduced width/height)
  - Use a subtler, dimmer visual style — no glow, thinner border
  - Completed wave nodes get green border (same as other completed nodes)

## Fix 4: Phase 2 Style Arrows
- **Idle edges**: Increase from 22% to 50-60% opacity, solid line (not dashed), strokeWidth 1.5
- **Completed edges**: Bold green (#10B981), strokeWidth 2.5, solid line, subtle glow
- **Running edges**: Keep animated cyan dashes
- Remove the floating dot animation on idle edges (too distracting)
- Add arrowhead markers (`<marker>` SVG def) so direction is always clear

## Files modified: 1
- `frontend/app/autonomous/page.tsx` — AgentNetworkGraph component only


---

## User Context

Do it robustly without messing anything else