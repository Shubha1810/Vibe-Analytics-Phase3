---
created: 2026-09-19T11:37:18.770Z
session: 4edf68f4-2e68-4863-b5a7-1b7fd25c8454
working_directory: C:\Users\2000167629\Vibe-Analytics-Phase3
---


# Autonomous Report Enhancements — Phase 3

## 1. Report Header & Storytelling Narrative
**File**: `page.tsx` (completed state section)
- Replace "Autonomous Intelligence Report" with "Autonomous Demand Sensing Report"
- Add executive narrative below: what the report is, what challenge it solves, how to interpret it
- Style like Phase 2's gradient banner header (Brightway Retail branding, not Clarins)

## 2. Persona Tabs — Sync with Global Dropdown
**Files**: `page.tsx`, `context/AppContext.tsx`
- Keep 3 persona tabs inside the report
- When user clicks a tab → also update global persona in AppContext
- When user changes global persona dropdown → update `activePersonaKey` to match
- Two-way sync so they never disagree

## 3. Improved "HOW TO READ IT" Sections
**File**: `HowToReadIt.tsx` + each chart component's bullets
- Rewrite bullets to be business-friendly: what the chart shows, how to interpret high/low values, what actions to derive
- Remove technical jargon, target executive stakeholders

## 4. Cortex AI Insights — Parse exec_report Properly
**File**: `ChartExplainer.tsx` (or new InsightBlock component)
- Parse the pipeline `exec_report` sections/narrative — extract headline, key_metrics, anomalies, drivers
- Format as: bold "CORTEX AI INSIGHT" header (visible, purple), "Strategic Insight:" lead, bulleted key findings with bold metrics
- Strip raw JSON — never show JSON to the user
- Ordered by impact: highest-impact first

## 5. Chart Axis Readability
**Files**: All Plotly chart components (DeviationHeatmap, VarianceHistogram, DriverAttribution, RecoveryTimeline)
- Clear business-friendly axis labels (not field names)
- Larger font sizes for axis text
- Add axis descriptions where needed

## 6. Warmer Color Palette
**Files**: All chart components + KPI cards + insight sections
- Soft orange, amber, warm blue accents
- Executive dashboard feel
- Apply to charts, KPI cards, insight blocks, highlighted findings

## 7. KPI Metric Explanations
**File**: `page.tsx` (KPICard component)
- Add 1-line explanation under each KPI value: what it means, why it matters
- E.g. "Total Anomalies" → "SKUs deviating >10% from forecast across all departments"

## Files to modify (~10):
1. `page.tsx` — header, narrative, KPI explanations, persona sync, insight formatting
2. `context/AppContext.tsx` — persona sync hook
3. `ChartExplainer.tsx` — proper exec_report parsing
4. `HowToReadIt.tsx` — business-friendly rewrites
5. `DeviationHeatmap.tsx` — axis labels, colors
6. `VarianceHistogram.tsx` — axis labels, colors
7. `DriverAttribution.tsx` — axis labels, colors
8. `RecoveryTimeline.tsx` — warmer colors, clearer labels
9. `AnomalyTable.tsx` — colors
10. `StepHeader.tsx` — minor styling

**Approach**: Use 3 parallel subagents to handle independent file groups, then build-verify.


---

## User Context

Make sure you do not change anything else and improve the qulaity of the report please